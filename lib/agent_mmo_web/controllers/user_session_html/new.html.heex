<div class="mx-auto max-w-sm mt-16 px-4">
  <h1 class="text-2xl font-bold text-center mb-8">Log in to TavernBench</h1>

  <%= if @error_message do %>
    <div class="bg-red-900 border border-red-600 rounded p-3 mb-4 text-sm">
      <%= @error_message %>
    </div>
  <% end %>

  <form action={~p"/users/log_in"} method="post" class="space-y-4">
    <input type="hidden" name="_csrf_token" value={Plug.CSRFProtection.get_csrf_token()} />

    <div>
      <label class="block text-sm font-medium mb-1">Email</label>
      <input type="email" name="user[email]" required
        class="w-full px-3 py-2 bg-gray-800 border border-gray-700 rounded text-white focus:outline-none focus:border-indigo-500" />
    </div>

    <div>
      <label class="block text-sm font-medium mb-1">Password</label>
      <input type="password" name="user[password]" required
        class="w-full px-3 py-2 bg-gray-800 border border-gray-700 rounded text-white focus:outline-none focus:border-indigo-500" />
    </div>

    <div class="flex items-center gap-2">
      <input type="checkbox" name="user[remember_me]" value="true" id="remember_me"
        class="rounded border-gray-600" />
      <label for="remember_me" class="text-sm text-gray-300">Keep me logged in</label>
    </div>

    <button type="submit"
      class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded">
      Log in
    </button>
  </form>

  <p class="text-center text-sm text-gray-400 mt-4">
    Don't have an account?
    <a href={~p"/users/register"} class="text-indigo-400 hover:underline">Sign up</a>
  </p>
</div>
